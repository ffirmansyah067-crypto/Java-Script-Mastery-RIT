// Deklarasi variabel
const namaProduk = "Sepatu Lari"; // Contoh nama produk, tidak wajib dipakai untuk perhitungan
const hargaProduk1 = 250000; // Harga produk pertama (Rp)
const hargaProduk2 = 175500; // Harga produk kedua (Rp)

// Hitung total harga
const totalHarga = hargaProduk1 + hargaProduk2;

// Tampilkan hasil
console.log(`Total harga untuk produk 1 (${hargaProduk1}) dan produk 2 (${hargaProduk2}) adalah Rp${totalHarga}`);

// Contoh lain dengan penamaan yang lebih umum (A dan B)
const namaProdukA = "Buku Catatan";
const namaProdukB = "Pulpen Set";
const hargaA = 45000;
const hargaB = 22500;

const totalAB = hargaA + hargaB;

console.log(`Total harga untuk produk ${namaProdukA} dan ${namaProdukB} adalah Rp${totalAB}`);